// Ejemplo de valores para desarrollo local o producción.
// Copia este archivo como 'environment.ts' y 'environment.prod.ts' y ajusta los valores.

export const environment = {
  production: false, // Cambia a true para el archivo de producción
  apiUrl: 'http://localhost:URL', // URL del Backend
};
